export type ShopCategoryNode = { slug: string; name: string; count: number; children: ShopCategoryNode[] };
export type BrandFacet = { name: string; count: number };
export type SortOption = { value: string; label: string };
